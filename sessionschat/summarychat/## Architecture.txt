   ## Architecture

   ```
   [Arduino Sensors] → [Python API] →
     ├→ InfluxDB (HOT)  — Real-time alerting, live dashboards
     ├→ PostgreSQL (WARM) — Crop lifecycle, client management, BI
     └→ Redis (CACHE)    — Latest readings (<5ms), dashboard refresh
   ```

   ## Test plan

   - [ ] `docker-compose up -d postgres redis` — both services start healthy
   - [ ] `psql -h localhost -U agritech -d agritech -c '\dn'` — 7 schemas exist
   - [ ] `python seed_varieties.py` — 6 varieties with expanded growth stages seeded
   - [ ] `python migrate_sqlite_to_postgres.py --dry-run` — migration previews correctly
   - [ ] Default `python server.py` — SQLite mode works unchanged
   - [ ] `DB_BACKEND=postgres python server.py` — PostgreSQL mode works
   - [ ] `POST /api/data` — data flows to InfluxDB + Redis cache
   - [ ] `GET /api/data/latest` — served from Redis (`_source: redis_cache`)
   - [ ] Existing tests still pass (`python -m pytest test_*.py`)

   🤖 Generated with [Claude Code](https://claude.com/claude-code)"--base master
   Create pull request